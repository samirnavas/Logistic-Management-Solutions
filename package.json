{
  "name": "logistic-management-solutions-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:admin": "npm run dev --workspace=@apps/admin",
    "dev:backend": "npm run dev --workspace=@apps/backend",
    "start:backend": "npm run start --workspace=@apps/backend"
  }
}